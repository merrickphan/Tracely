// Temporary. Exists to prove the `check` workflow actually goes red before we
// start requiring it on pull requests — a required check that has never failed
// is a rubber stamp, not a gate. Delete this file and the branch once the run
// is confirmed red.
export const deliberatelyWrong: number = 'this is a string, not a number'
